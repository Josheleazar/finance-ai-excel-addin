/*
 * AI Insights tab — placeholder.
 *
 * Planned:
 *   - Anomaly detection over imported time series (z-score / IQR).
 *   - Natural-language explanations of movements (LLM call via a backend).
 *   - Flag outliers directly in the sheet with comments/formatting.
 */

export function initAiInsightsTab(): void {
  // No interactive logic yet. The static markup lives in taskpane.html.
}
