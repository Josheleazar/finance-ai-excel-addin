/*
 * Reconciliation tab — placeholder.
 *
 * Planned MVP:
 *   - User selects two ranges (source, target) in the workbook.
 *   - We build multisets and compute: matches, source-only, target-only, duplicates.
 *   - Results written to a new sheet with a summary row.
 *   - Optional numeric tolerance for near-match comparisons.
 */

export function initReconciliationTab(): void {
  // No interactive logic yet. The static markup lives in taskpane.html.
}
